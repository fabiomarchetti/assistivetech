# scrivi con le sillabe

Esercizio di Training Cognitivo - AssistiveTech.it

## Informazioni

- **ID Esercizio**: 19
- **Nome**: scrivi con le sillabe
- **Tipo**: Progressive Web App (PWA) Flutter
- **Generato**: Automaticamente dal sistema AssistiveTech

## Sviluppo

Questa app Flutter è stata generata automaticamente. Per sviluppare l'esercizio:

1. Modifica `lib/main.dart` per implementare la logica specifica
2. Aggiungi asset necessari in `assets/`
3. Testa con `flutter run -d web-server --web-port=8080`
4. Build per produzione con `flutter build web --release`

## Comandi Flutter

```bash
# Installa dipendenze
flutter pub get

# Avvia in modalità debug web
flutter run -d web-server --web-port=8080

# Build per produzione
flutter build web --release

# Test
flutter test

# Analisi codice
flutter analyze
```

## Deployment

L'app viene automaticamente deployata in:
- **URL**: https://assistivetech.it/training_cognitivo/[categoria]/scrivi con le sillabe/
- **PWA**: Installabile come app standalone

## Note di Sviluppo

- Utilizzare Material Design per consistenza UI
- Implementare gestione responsive per tablet
- Aggiungere logging delle sessioni utente
- Integrare con API AssistiveTech per dati utente